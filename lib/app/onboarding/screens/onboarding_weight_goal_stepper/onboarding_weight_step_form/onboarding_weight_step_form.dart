import 'package:flutter/material.dart';

class OnboardingWeightStepForm extends StatelessWidget {
  const OnboardingWeightStepForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
